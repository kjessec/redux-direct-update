{
  "name": "redux-direct-update",
  "version": "3.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preversion": "rm -rf dist && mkdir dist && babel src --out-dir dist && git add ."
  },
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "redux": "^3.6.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ],
    "plugins": [
      "@babel/plugin-syntax-object-rest-spread"
    ]
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@babel/cli": "^7.2.0",
    "@babel/core": "^7.2.0",
    "@babel/plugin-syntax-object-rest-spread": "^7.2.0",
    "@babel/preset-env": "^7.2.0",
    "redux": "^3.6.0",
    "tape": "^4.6.3"
  },
  "dependencies": {
    "dot-prop-immutable": "^1.5.0"
  }
}
