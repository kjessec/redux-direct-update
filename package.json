{
  "name": "redux-direct-update",
  "version": "2.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preversion": "babel src --out-dir dist && git add ."
  },
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "redux": "^3.6.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-es2015"
    ],
    "plugins": [
      "transform-object-rest-spread"
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.2.0",
    "@babel/plugin-syntax-object-rest-spread": "^7.2.0",
    "@babel/preset-es2015": "^7.0.0-beta.53",
    "babel-eslint": "^10.0.1",
    "redux": "^3.6.0",
    "tape": "^4.6.3"
  },
  "dependencies": {
    "dot-prop-immutable": "^1.5.0"
  }
}
